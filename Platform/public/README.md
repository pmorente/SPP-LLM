# Background Image

Place your background image in this folder and name it `background.jpg`.

The image will be used as the background for all pages in the application.

Supported formats: JPG, PNG, WebP

If you want to use a different filename, update the `bg-landscape` class in `src/index.css`.

